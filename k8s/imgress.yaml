apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: movie-backend-ingress
spec:
  rules:
  - host: your-domain.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: movie-backend
            port:
              number: 8081